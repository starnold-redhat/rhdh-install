---
- name: Bootstrap Backstage
  hosts: localhost 
  connection: local

  roles:
    - name: install-operators 
    - name: install-gitlab
    - name: configure-gitlab
    - name: install-sso
    - name: configure-backstage    
